/**
 * @file Declares Tag data type representing a string that begins with a '#'
 * @typedef Tag Represents a string that begins with a '#'
 * @property {string} tag word to tag a tuit with
 */
export default class Tag {
    private tag: string = "#";
}